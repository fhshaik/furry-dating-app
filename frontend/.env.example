# Backend API URL used by the Vite dev server proxy (server-side only)
VITE_API_URL=http://localhost:8000

# Base URL prefix for Axios requests (client-side)
# Defaults to /api, which is proxied to VITE_API_URL in dev
# and routed via nginx in production
VITE_API_BASE_URL=/api
